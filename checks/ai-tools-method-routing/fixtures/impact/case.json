{
  "change": {
    "file": "producer.nix",
    "symbol": "widgetSchema",
    "from": 1,
    "to": 2
  },
  "expected_consumers": ["consumer.nix"],
  "discriminating_check": {
    "description": "The changed contract is read only by consumer.nix, not by unrelated.nix.",
    "command": "grep -q 'producer.contract.widgetSchema'",
    "affected": "consumer.nix",
    "unaffected": "unrelated.nix"
  }
}
